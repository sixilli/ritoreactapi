
export async function request(key: String, param: String) {
    // Return various components based on user selection
    switch(param) {
        case param = 'summonerName':
            break

        case param = 'rankedSolo':
            break

        case param = 'match':
            break
    }

}